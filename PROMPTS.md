1. How do I structure a clean global state object in vanilla JS to manage a primary numerical value (salary) alongside an array of sub-objects (expenses) without tightly coupling the data to the HTML DOM?

2. Write a lightweight helper function to sanitize user text strings before inserting them into a table using innerHTML, specifically to block script injections or HTML breaking.

3. I am getting an error with Chart.js when re-initializing a canvas element on data updates. How do I track and destroy the existing chart instance dynamically inside a render function?

4. What is the syntax for pulling specific conversion data using the browser's native fetch API from api.frankfurter.app, and how do I handle the asynchronous response safely if the network fails?

5. Show me how to use the browser's built-in Intl.NumberFormat utility to format currency based on a conditional state check, switching between Indian standard formatting and US standard formatting.

6. How do I write a clean event delegation or array filtering mechanism to remove a specific item from a state array using a unique timestamp ID, then force a complete visual update of the UI list?

7. My forms are refreshing the entire web page on click. How do I block the default form execution thread in JavaScript, and what is the best way to handle global validation errors in a single designated alert box?
8. README Documentation-Help me structure a professional README.md file